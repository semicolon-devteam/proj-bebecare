# Epic 2: AI 기반 맞춤 정보 제공 엔진

> 사용자 프로필 기반으로 개인화된 조언을 제공하는 AI 챗봇 및 맞춤 조언 시스템

**Epic ID**: EPIC-002
**상태**: Planned
**우선순위**: P0 (최우선)
**예상 기간**: 2주 (Sprint 4)
**의존성**: Epic 1 (사용자 프로필) 완료 필요

---

## 📋 개요

### 목적
OpenAI GPT-4o-mini를 활용하여 사용자 프로필(임신 주차, 자녀 개월, 직장, 지역)에 맞춘 개인화된 육아 조언 제공

### 핵심 가치
- **차별화 포인트**: 기존 육아 앱에는 없는 AI 기반 맞춤 조언
- **편의성**: 검색 없이 질문만 하면 즉시 답변
- **신뢰성**: 사용자 프로필 기반으로 정확한 맥락의 답변

---

## 🎯 사용자 스토리

### Story 1: AI 챗봇 대화
**As a** 임신부
**I want to** "출산휴가는 언제부터 쓸 수 있나요?"라고 질문하여
**So that** 내 출산예정일 기준으로 정확한 답변을 받을 수 있다

**Acceptance Criteria**:
- [ ] 챗봇 UI에서 질문 입력
- [ ] AI가 사용자 프로필(임신 주차, 출산예정일, 직장)을 고려한 답변 생성
- [ ] 5초 이내 응답
- [ ] 대화 히스토리 저장 (이전 대화 불러오기)

**예시 대화**:
```
사용자: "출산휴가는 언제부터 쓸 수 있나요?"
AI: "출산예정일이 2026년 6월 15일이시네요. 출산휴가는 출산예정일 45일 전부터 사용 가능하므로,
     귀하의 경우 2026년 5월 1일부터 출산휴가를 시작할 수 있습니다.
     출산 후 45일까지 총 90일(다태아는 120일)의 휴가를 사용하실 수 있습니다."
```

---

### Story 2: 발달 단계 질문 (신생아 부모)
**As a** 신생아 부모
**I want to** "우리 아기가 목을 아직 못 가누는데 괜찮나요?"라고 질문하여
**So that** 아이 개월 수에 맞는 발달 정보를 받을 수 있다

**Acceptance Criteria**:
- [ ] AI가 자녀 생년월일 기반으로 개월 수 계산
- [ ] 해당 개월 수의 정상 발달 범위 안내
- [ ] 필요 시 소아과 상담 권장

**예시 대화**:
```
사용자: "우리 아기가 목을 아직 못 가누는데 괜찮나요?"
AI: "현재 생후 3개월이시네요. 일반적으로 아기는 생후 3-4개월에 목을 가누기 시작합니다.
     아직 3개월이므로 정상 범위 내에 있습니다.
     만약 4개월 이후에도 목을 가누지 못한다면 소아과 상담을 권장드립니다."
```

---

### Story 3: 홈 화면 맞춤 조언 카드
**As a** 사용자
**I want to** 홈 화면에 "지금 알아야 할 정보"를 자동으로 보여주어
**So that** 검색 없이 필요한 정보를 빠르게 확인할 수 있다

**Acceptance Criteria**:
- [ ] 홈 화면에 2-3개의 맞춤 조언 카드 표시
- [ ] 임신 주차/아이 개월 수에 맞는 정보 자동 생성
- [ ] 카드 클릭 시 상세 정보 또는 AI 챗봇 연결

**예시 카드**:
```
📌 28주차 체크리스트
- 태아보험 가입 마감이 얼마 남지 않았습니다 (22주 권장)
- 임신 당뇨 검사를 24-28주 사이에 받으세요
- 출산 준비 클래스 등록을 고려해보세요
```

---

### Story 4: 대화 히스토리 관리
**As a** 사용자
**I want to** 이전 대화 내용을 다시 확인하여
**So that** 받았던 조언을 나중에 참고할 수 있다

**Acceptance Criteria**:
- [ ] 대화 히스토리 자동 저장 (Supabase)
- [ ] 챗봇 화면에서 이전 대화 스크롤하여 확인
- [ ] 대화 검색 기능 (선택사항)

---

## 🛠️ 기술 요구사항

### AI 프롬프트 전략

#### 시스템 프롬프트 (System Prompt)
```
당신은 임신·출산·육아 전문 AI 어시스턴트 "BebeCare"입니다.

역할:
- 임신부와 육아 부모에게 신뢰할 수 있는 정보를 제공합니다
- 의료 진단이나 처방은 하지 않으며, 참고용 정보만 제공합니다
- 필요 시 전문가 상담을 권장합니다

답변 원칙:
1. 사용자 프로필(임신 주차, 자녀 개월, 직장, 지역)을 고려하여 맞춤 답변
2. 정확하고 명확한 정보 제공 (출처 명시 가능 시)
3. 의료 정보는 "참고용"임을 명시하고 전문가 상담 권장
4. 친근하고 공감하는 톤 사용
5. 한국 상황(정부 정책, 육아 문화)에 맞춘 답변
```

#### 사용자 컨텍스트 주입
```typescript
const userContext = `
사용자 정보:
- 임신 주차: ${profile.pregnancy_week}주
- 출산예정일: ${profile.due_date}
- 직장: ${profile.is_working ? '근무 중' : '비근무'}
- 지역: ${profile.region_province} ${profile.region_city}
${children.length > 0 ? `
- 자녀: ${children.map(c => `${c.name} (생후 ${c.age_months}개월, ${c.gender})`).join(', ')}
` : ''}
`;
```

#### 프롬프트 템플릿
```typescript
const messages = [
  { role: 'system', content: systemPrompt },
  { role: 'user', content: `${userContext}\n\n질문: ${userQuery}` }
];
```

---

### 데이터베이스 스키마

```sql
-- AI 대화 히스토리 테이블
CREATE TABLE conversations (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE NOT NULL,

  -- 대화 내용
  messages JSONB NOT NULL DEFAULT '[]'::jsonb,
  -- 형식: [{ role: 'user'|'assistant', content: string, timestamp: string }]

  -- 메타데이터
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- 인덱스
CREATE INDEX idx_conversations_user_id ON conversations(user_id);
CREATE INDEX idx_conversations_updated_at ON conversations(updated_at DESC);

-- RLS 정책
ALTER TABLE conversations ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Users can view own conversations"
  ON conversations FOR SELECT
  USING (auth.uid() = user_id);

CREATE POLICY "Users can insert own conversations"
  ON conversations FOR INSERT
  WITH CHECK (auth.uid() = user_id);

CREATE POLICY "Users can update own conversations"
  ON conversations FOR UPDATE
  USING (auth.uid() = user_id);
```

---

### API 엔드포인트

#### 1. AI 챗봇 질문
```typescript
// POST /api/ai/chat
// AI에게 질문하고 답변 받기
Request: {
  message: string; // 사용자 질문
  conversation_id?: string; // 기존 대화 이어가기 (선택)
}

Response: {
  conversation_id: string;
  message: string; // AI 답변
  timestamp: string;
}

// 구현 예시 (Next.js API Route)
export async function POST(request: Request) {
  const { message, conversation_id } = await request.json();
  const user = await getUser(); // Supabase Auth

  // 1. 사용자 프로필 조회
  const profile = await supabase
    .from('profiles')
    .select('*')
    .eq('user_id', user.id)
    .single();

  // 2. 자녀 정보 조회
  const { data: children } = await supabase
    .from('children')
    .select('*')
    .eq('user_id', user.id);

  // 3. 프롬프트 생성
  const userContext = generateUserContext(profile, children);
  const systemPrompt = getSystemPrompt();

  // 4. OpenAI API 호출
  const completion = await openai.chat.completions.create({
    model: 'gpt-4o-mini',
    messages: [
      { role: 'system', content: systemPrompt },
      { role: 'user', content: `${userContext}\n\n질문: ${message}` }
    ],
    temperature: 0.7,
    max_tokens: 500
  });

  const aiMessage = completion.choices[0].message.content;

  // 5. 대화 히스토리 저장
  const conversation = await saveConversation(user.id, conversation_id, [
    { role: 'user', content: message, timestamp: new Date() },
    { role: 'assistant', content: aiMessage, timestamp: new Date() }
  ]);

  return { conversation_id: conversation.id, message: aiMessage, timestamp: new Date() };
}
```

---

#### 2. 홈 화면 맞춤 조언 생성
```typescript
// GET /api/ai/advice
// 홈 화면 맞춤 조언 카드 생성
Response: {
  advice_cards: Array<{
    title: string;
    content: string;
    category: 'pregnancy' | 'baby' | 'government' | 'childcare';
  }>;
}

// 구현 로직
// - 임신 주차별 또는 아이 개월별로 미리 정의된 조언 템플릿 사용
// - 사용자 프로필에 맞춰 동적 생성
// - 캐싱 전략 (동일 주차/개월은 1일 캐시)
```

---

#### 3. 대화 히스토리 조회
```typescript
// GET /api/conversations
// 사용자의 모든 대화 조회
Response: {
  conversations: Array<{
    id: string;
    messages: Array<{
      role: 'user' | 'assistant';
      content: string;
      timestamp: string;
    }>;
    created_at: string;
  }>;
}
```

---

## 🎨 UI/UX 요구사항

### AI 챗봇 화면
- **레이아웃**: 메시지 리스트 + 입력창 (하단 고정)
- **메시지 버블**: 사용자 (오른쪽, 파란색), AI (왼쪽, 회색)
- **입력창**: 텍스트 입력 + 전송 버튼
- **로딩 상태**: AI 응답 대기 중 "..." 애니메이션 표시
- **스크롤**: 새 메시지 수신 시 자동 스크롤 (하단)

### 홈 화면 맞춤 조언 카드
- **레이아웃**: 카드 2-3개 (스크롤 가능)
- **디자인**: 아이콘 + 제목 + 간단한 설명
- **클릭 액션**: 상세 정보 모달 또는 AI 챗봇 이동

---

## 📊 성공 지표

### 핵심 KPI
- **AI 챗봇 사용률**: MAU 대비 50% 이상 (최소 1회 이상 사용)
- **AI 답변 만족도**: 4.0/5.0 이상 (대화 후 평가)
- **평균 대화 길이**: 3턴 이상 (사용자-AI 왕복)
- **AI 응답 시간**: 5초 이내

### 측정 방법
- Supabase Analytics 또는 Google Analytics 이벤트 트래킹
- 대화 후 만족도 평가 팝업 (선택사항)
- OpenAI API 응답 시간 로깅

---

## 🧪 테스트 계획

### 단위 테스트
- [ ] 프롬프트 생성 로직 (사용자 컨텍스트 주입)
- [ ] 대화 히스토리 저장 로직

### 통합 테스트
- [ ] OpenAI API 호출 성공/실패 처리
- [ ] 대화 히스토리 CRUD API

### E2E 테스트 (Playwright)
- [ ] AI 챗봇 질문 → 답변 받기 플로우
- [ ] 대화 히스토리 확인 플로우
- [ ] 홈 화면 맞춤 조언 카드 표시

### AI 답변 품질 테스트
- [ ] 임신부 질문 10개 (출산휴가, 태아보험 등) 테스트
- [ ] 신생아 부모 질문 10개 (예방접종, 발달 등) 테스트
- [ ] 영유아 부모 질문 10개 (어린이집, 교육 등) 테스트
- [ ] 프로필 반영 여부 확인 (개월 수, 지역 등)

---

## 🚀 구현 계획

### Sprint 4 (2주)

#### Week 1: OpenAI 연동 및 챗봇 UI
- **Day 1**: OpenAI API 키 설정, 환경 변수
- **Day 2**: 프롬프트 템플릿 작성 및 테스트
- **Day 3-4**: AI 챗봇 UI 구현 (메시지 리스트, 입력창)
- **Day 5**: AI 챗봇 API 구현 (POST /api/ai/chat)

#### Week 2: 대화 히스토리 및 맞춤 조언
- **Day 1-2**: 대화 히스토리 저장/조회 구현
- **Day 3**: 홈 화면 맞춤 조언 카드 API 및 UI
- **Day 4**: AI 답변 품질 테스트 및 프롬프트 튜닝
- **Day 5**: 테스트 및 버그 수정

---

## 📝 체크리스트

### 개발 완료 조건
- [ ] OpenAI API 연동 완료
- [ ] 프롬프트 템플릿 작성 (임신부, 신생아, 영유아)
- [ ] AI 챗봇 UI 구현 (메시지, 입력, 로딩)
- [ ] 대화 히스토리 저장/조회 구현
- [ ] 홈 화면 맞춤 조언 카드 구현
- [ ] E2E 테스트 작성 (핵심 플로우)

### 품질 검증 조건
- [ ] AI 응답 시간 <5초
- [ ] AI 답변 만족도 >4.0/5.0 (내부 테스트)
- [ ] 프로필 정보 반영 확인 (임신 주차, 개월 수 등)
- [ ] 의료 정보 면책 문구 표시

---

## 💰 비용 예측

### OpenAI API 비용
- **모델**: GPT-4o-mini
- **가격**: $0.15 / 1M input tokens, $0.60 / 1M output tokens
- **예상 사용량** (1,000 MAU 기준):
  - 1인당 월 10회 질문
  - 1회당 평균 입력 300 tokens, 출력 200 tokens
  - 월 총 사용: 3M input tokens + 2M output tokens
  - **월 예상 비용**: $0.45 (input) + $1.20 (output) = **$1.65 (약 2,200원)**

---

## 📚 참고 자료

- [OpenAI API 문서](https://platform.openai.com/docs/api-reference)
- [GPT-4o-mini 가이드](https://platform.openai.com/docs/models/gpt-4o-mini)
- [프롬프트 엔지니어링 가이드](https://platform.openai.com/docs/guides/prompt-engineering)

---

**작성일**: 2026-02-07
**작성자**: BebeCare Team
