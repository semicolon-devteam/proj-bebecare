# 기술 스택 (Tech Stack)

> BebeCare 프로젝트 - 기술 선정 및 아키텍처 결정

**작성일**: 2026-02-07
**버전**: 1.0
**작성자**: BebeCare Team

---

## 📋 목차

1. [기술 스택 개요](#1-기술-스택-개요)
2. [Frontend](#2-frontend)
3. [Backend](#3-backend)
4. [AI](#4-ai)
5. [Infrastructure](#5-infrastructure)
6. [개발 도구](#6-개발-도구)
7. [아키텍처 다이어그램](#7-아키텍처-다이어그램)

---

## 1. 기술 스택 개요

### 1.1 전체 스택

```
┌─────────────────────────────────────────────┐
│               Frontend (Vercel)              │
│  Next.js 15 + React 19 + TypeScript          │
│  Tailwind CSS + shadcn/ui                    │
└─────────────────────────────────────────────┘
                      ↓ ↑
┌─────────────────────────────────────────────┐
│             Backend (Supabase)               │
│  PostgreSQL + Auth + Storage + Realtime      │
│  Row Level Security (RLS)                    │
└─────────────────────────────────────────────┘
                      ↓ ↑
┌─────────────────────────────────────────────┐
│                AI (OpenAI)                   │
│            GPT-4o-mini API                   │
└─────────────────────────────────────────────┘
```

---

### 1.2 선정 기준

1. **빠른 개발**: 8주 개발 일정에 맞춘 생산성
2. **확장성**: 사용자 증가 시 수평 확장 가능
3. **비용 효율**: 초기 무료 티어 활용, 점진적 확장
4. **개발자 경험**: 최신 기술 스택, 풍부한 문서
5. **보안**: 내장 인증, RLS, HTTPS 기본 제공

---

## 2. Frontend

### 2.1 Core Framework

#### Next.js 15 (App Router)
**선정 이유**:
- **React Server Components**: SSR/ISR로 성능 최적화
- **App Router**: 파일 기반 라우팅, 레이아웃 재사용
- **Image Optimization**: 자동 이미지 최적화 (WebP, 반응형)
- **API Routes**: 프록시 서버 역할, OpenAI API 키 숨기기
- **Vercel 통합**: 자동 배포, Edge Functions

**버전**: `15.1.x` (최신 안정 버전)

**주요 기능 활용**:
- SSR: 개인화 페이지 (홈, 타임라인)
- ISR: 정적 콘텐츠 (정보 허브)
- CSR: AI 챗봇 (실시간 상호작용)

---

#### React 19
**선정 이유**:
- **Concurrent Features**: Suspense, Transitions로 부드러운 UX
- **Server Components**: 서버에서 렌더링, 번들 크기 감소
- **Automatic Batching**: 상태 업데이트 최적화

**버전**: `19.x` (Next.js 15 권장 버전)

---

#### TypeScript
**선정 이유**:
- **타입 안전성**: 런타임 에러 사전 방지
- **개발 생산성**: 자동 완성, 리팩토링 지원
- **Supabase 통합**: Supabase 자동 타입 생성

**버전**: `5.x`

**tsconfig.json 설정**:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "jsx": "preserve",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "incremental": true,
    "plugins": [{ "name": "next" }],
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

---

### 2.2 UI Framework

#### Tailwind CSS
**선정 이유**:
- **유틸리티 우선**: 빠른 스타일링, 일관된 디자인
- **반응형**: 모바일 우선, 간편한 반응형 클래스
- **커스터마이징**: 디자인 토큰 (색상, 간격 등) 설정 가능
- **번들 최적화**: PurgeCSS로 미사용 스타일 제거

**버전**: `4.x`

**tailwind.config.ts 설정**:
```typescript
import type { Config } from 'tailwindcss';

const config: Config = {
  content: [
    './src/pages/**/*.{js,ts,jsx,tsx,mdx}',
    './src/components/**/*.{js,ts,jsx,tsx,mdx}',
    './src/app/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        primary: '#3B82F6', // 파란색 (건강)
        secondary: '#F59E0B', // 주황색 (보험)
        success: '#10B981', // 초록색 (완료)
        warning: '#EF4444', // 빨간색 (높은 우선순위)
      },
    },
  },
  plugins: [],
};

export default config;
```

---

#### shadcn/ui
**선정 이유**:
- **복사 가능한 컴포넌트**: 라이브러리가 아닌 코드 복사 방식
- **Radix UI 기반**: 접근성 (WCAG 2.1 AA) 기본 제공
- **커스터마이징 용이**: 프로젝트에 맞게 수정 가능
- **Tailwind CSS 통합**: 스타일 일관성

**버전**: 최신 (CLI로 설치)

**주요 컴포넌트**:
- Button, Input, Select (폼 요소)
- Dialog, Sheet (모달, 사이드바)
- Calendar, DatePicker (날짜 선택)
- Card, Badge (정보 표시)

---

### 2.3 상태 관리

#### Zustand
**선정 이유**:
- **간단한 API**: Redux보다 단순, 보일러플레이트 적음
- **TypeScript 지원**: 타입 안전한 스토어
- **번들 크기**: 1KB (gzip)
- **Server Components 호환**: 클라이언트 컴포넌트에서만 사용

**버전**: `5.x`

**사용 예시**:
```typescript
// src/stores/user-store.ts
import { create } from 'zustand';

interface UserState {
  profile: Profile | null;
  setProfile: (profile: Profile) => void;
}

export const useUserStore = create<UserState>((set) => ({
  profile: null,
  setProfile: (profile) => set({ profile }),
}));
```

**대안**: React Context API (간단한 상태는 Context로 충분)

---

### 2.4 폼 관리

#### React Hook Form
**선정 이유**:
- **성능**: 비제어 컴포넌트 기반, 리렌더링 최소화
- **유효성 검사**: Zod 통합으로 타입 안전한 검증
- **개발자 경험**: 간단한 API, TypeScript 지원

**버전**: `7.x`

**사용 예시**:
```typescript
import { useForm } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { z } from 'zod';

const schema = z.object({
  due_date: z.string().min(1, '출산예정일을 선택하세요'),
  is_working: z.boolean(),
});

const { register, handleSubmit, formState: { errors } } = useForm({
  resolver: zodResolver(schema),
});
```

---

### 2.5 기타 라이브러리

| 라이브러리 | 용도 | 버전 |
|-----------|------|------|
| **date-fns** | 날짜 계산 (임신 주차, 아이 개월) | `4.x` |
| **react-hot-toast** | 토스트 알림 (성공/에러 메시지) | `2.x` |
| **lucide-react** | 아이콘 | `latest` |
| **clsx** / **tailwind-merge** | 조건부 클래스명 유틸 | `latest` |

---

## 3. Backend

### 3.1 Supabase

**선정 이유**:
- **All-in-One**: PostgreSQL + Auth + Storage + Realtime 통합
- **빠른 설정**: 5분 이내 프로젝트 생성 및 연동
- **무료 티어**: 500MB DB, 1GB 파일 저장, 50,000 MAU
- **보안**: Row Level Security (RLS) 기본 제공
- **TypeScript**: 자동 타입 생성 (`supabase gen types`)

**버전**: Cloud (최신)

---

#### PostgreSQL
**용도**: 메인 데이터베이스

**주요 테이블**:
- `profiles`: 사용자 프로필 (임신 정보, 직장, 지역)
- `children`: 자녀 정보 (생년월일, 성별)
- `conversations`: AI 대화 히스토리
- `timelines`: 시기별 체크리스트

**기능 활용**:
- Generated Columns: 임신 주차, 아이 개월 자동 계산
- RLS: 사용자별 데이터 접근 제어
- Triggers: 타임라인 자동 생성 (프로필 생성 시)

---

#### Supabase Auth
**용도**: 사용자 인증 및 권한 관리

**지원 방식**:
- 이메일/비밀번호
- 소셜 로그인 (Google, Apple - 선택사항)

**보안**:
- JWT 토큰 기반
- RLS와 연동 (`auth.uid()`)
- HTTPS 기본

---

#### Supabase Storage
**용도**: 사용자 업로드 파일 (프로필 사진 등, 선택사항)

**기능**:
- 파일 업로드/다운로드
- 이미지 변환 (리사이징)
- RLS 적용 가능

---

#### Supabase Realtime
**용도**: 푸시 알림 (선택사항)

**기능**:
- Database Changes 구독
- 실시간 업데이트

---

### 3.2 Supabase Edge Functions

**용도**: 서버리스 함수 (푸시 알림 Cron Job)

**언어**: TypeScript (Deno 런타임)

**예시**: 매일 오전 9시 타임라인 알림 전송
```typescript
// supabase/functions/send-notifications/index.ts
import { serve } from 'https://deno.land/std@0.168.0/http/server.ts';

serve(async (req) => {
  // 알림 로직
});
```

---

## 4. AI

### 4.1 OpenAI API

**선정 이유**:
- **GPT-4o-mini**: 빠른 응답, 저렴한 비용
- **안정성**: 높은 가용성, API 신뢰성
- **한국어 지원**: 자연스러운 한국어 답변
- **간단한 통합**: REST API, 공식 SDK

**버전**: `gpt-4o-mini` (최신)

**비용** (2026년 2월 기준):
- Input: $0.15 / 1M tokens
- Output: $0.60 / 1M tokens
- 예상 비용: 월 $1.65 (1,000 MAU 기준)

---

#### OpenAI SDK
**언어**: TypeScript

**버전**: `4.x`

**사용 예시**:
```typescript
import OpenAI from 'openai';

const openai = new OpenAI({
  apiKey: process.env.OPENAI_API_KEY,
});

const completion = await openai.chat.completions.create({
  model: 'gpt-4o-mini',
  messages: [
    { role: 'system', content: systemPrompt },
    { role: 'user', content: userQuery }
  ],
  temperature: 0.7,
  max_tokens: 500,
});
```

---

### 4.2 대안 고려 (Phase 2 이후)

#### Vector DB (RAG)
**용도**: 콘텐츠 기반 정확한 답변

**후보**:
- Supabase Vector (pgvector): Supabase 통합
- Pinecone: 전문 Vector DB

**도입 시기**: MVP 이후 (Phase 2)

---

## 5. Infrastructure

### 5.1 Vercel

**선정 이유**:
- **Next.js 최적화**: Next.js 개발사, 완벽한 통합
- **자동 배포**: GitHub Push 시 자동 배포
- **Edge Functions**: 전 세계 CDN 배포
- **무료 티어**: 개인 프로젝트 무료 (상용 시 Pro)

**기능 활용**:
- Preview Deployments: PR별 미리보기 배포
- Analytics: 성능 모니터링
- Environment Variables: API 키 안전 관리

---

### 5.2 Domain & SSL

**Domain**: TBD (예: bebecare.co.kr)

**SSL**: Vercel 자동 제공 (Let's Encrypt)

---

### 5.3 Monitoring & Logging

#### Sentry (에러 모니터링)
**선정 이유**:
- 실시간 에러 트래킹
- Source Map 지원
- 무료 티어: 5,000 errors/month

**버전**: `@sentry/nextjs`

---

#### Vercel Analytics (성능 모니터링)
**기능**:
- Web Vitals (LCP, FID, CLS)
- Real User Monitoring (RUM)

---

### 5.4 Notification Service (푸시 알림)

#### OneSignal 또는 Firebase Cloud Messaging

**선정 예정**: OneSignal
**이유**:
- 무료 티어: 10,000 subscribers
- Web Push 지원
- 간단한 통합

**대안**: Firebase Cloud Messaging (FCM)

---

## 6. 개발 도구

### 6.1 Package Manager

**pnpm**
**선정 이유**:
- 빠른 설치 속도
- 디스크 공간 절약 (심볼릭 링크)
- Monorepo 지원

**버전**: `9.x`

---

### 6.2 Code Quality

#### ESLint
**용도**: 코드 린팅

**설정**: Next.js 기본 설정 + TypeScript

**버전**: `9.x`

---

#### Prettier
**용도**: 코드 포맷팅

**설정**:
```json
{
  "semi": true,
  "singleQuote": true,
  "trailingComma": "es5",
  "printWidth": 100
}
```

---

### 6.3 Testing

#### Jest + React Testing Library
**용도**: 단위 테스트, 통합 테스트

**버전**: `jest@29.x`, `@testing-library/react@16.x`

---

#### Playwright
**용도**: E2E 테스트

**버전**: `1.x`

---

### 6.4 Git Hooks

#### Husky + lint-staged
**용도**: 커밋 전 자동 린트/포맷팅

**설정**:
```json
{
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{ts,tsx}": ["eslint --fix", "prettier --write"]
  }
}
```

---

## 7. 아키텍처 다이어그램

### 7.1 전체 아키텍처

```
┌───────────────────────────────────────────────────────────┐
│                       User (Browser)                       │
└───────────────────────────────────────────────────────────┘
                              ↓ ↑ HTTPS
┌───────────────────────────────────────────────────────────┐
│                    Vercel Edge Network                     │
│  ┌─────────────────────────────────────────────────────┐  │
│  │           Next.js 15 (App Router)                   │  │
│  │                                                     │  │
│  │  ┌──────────────┐  ┌──────────────┐               │  │
│  │  │ SSR Pages    │  │ API Routes   │               │  │
│  │  │ (홈, 타임라인)│  │ (/api/*)     │               │  │
│  │  └──────────────┘  └──────────────┘               │  │
│  │                                                     │  │
│  │  ┌──────────────┐  ┌──────────────┐               │  │
│  │  │ ISR Pages    │  │ CSR Pages    │               │  │
│  │  │ (콘텐츠)     │  │ (AI 챗봇)    │               │  │
│  │  └──────────────┘  └──────────────┘               │  │
│  └─────────────────────────────────────────────────────┘  │
└───────────────────────────────────────────────────────────┘
                              ↓ ↑
┌───────────────────────────────────────────────────────────┐
│                     Supabase Cloud                         │
│  ┌─────────────────────────────────────────────────────┐  │
│  │  PostgreSQL (Database)                              │  │
│  │  - profiles, children, conversations, timelines     │  │
│  │  - RLS (Row Level Security)                         │  │
│  └─────────────────────────────────────────────────────┘  │
│  ┌─────────────────────────────────────────────────────┐  │
│  │  Auth (JWT)                                         │  │
│  │  - Email/Password, Social Login                     │  │
│  └─────────────────────────────────────────────────────┘  │
│  ┌─────────────────────────────────────────────────────┐  │
│  │  Storage (Files)                                    │  │
│  │  - Profile Images (선택사항)                       │  │
│  └─────────────────────────────────────────────────────┘  │
│  ┌─────────────────────────────────────────────────────┐  │
│  │  Edge Functions (Cron)                              │  │
│  │  - send-notifications (매일 오전 9시)              │  │
│  └─────────────────────────────────────────────────────┘  │
└───────────────────────────────────────────────────────────┘
                              ↓ ↑
┌───────────────────────────────────────────────────────────┐
│                      OpenAI API                            │
│  - GPT-4o-mini (Chat Completions)                         │
│  - 프롬프트 기반 맞춤 조언                                │
└───────────────────────────────────────────────────────────┘
```

---

### 7.2 데이터 플로우 (AI 챗봇 예시)

```
[사용자] "출산휴가는 언제부터?"
    ↓
[Next.js API Route] POST /api/ai/chat
    ↓
[1] Supabase Auth: 사용자 인증 확인
    ↓
[2] Supabase DB: 프로필 조회 (출산예정일 등)
    ↓
[3] 프롬프트 생성: "출산예정일: 2026-06-15, 질문: ..."
    ↓
[4] OpenAI API: GPT-4o-mini 호출
    ↓
[5] AI 답변: "출산휴가는 5월 1일부터..."
    ↓
[6] Supabase DB: 대화 히스토리 저장
    ↓
[Next.js API Route] 응답 반환
    ↓
[사용자] AI 답변 표시
```

---

## 8. 환경 변수

### 8.1 필수 환경 변수

```bash
# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://[project-id].supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=[anon-key]
SUPABASE_SERVICE_ROLE_KEY=[service-role-key]

# OpenAI
OPENAI_API_KEY=sk-[api-key]

# Vercel (자동 설정)
NEXT_PUBLIC_VERCEL_URL=[auto-generated]

# Sentry (선택사항)
NEXT_PUBLIC_SENTRY_DSN=[dsn]

# Notification (선택사항)
ONESIGNAL_APP_ID=[app-id]
ONESIGNAL_API_KEY=[api-key]
```

---

### 8.2 환경별 설정

- **Development**: `.env.local` (Git 제외)
- **Production**: Vercel Dashboard 환경 변수

---

## 9. 버전 관리

### 9.1 package.json (주요 의존성)

```json
{
  "name": "bebecare",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "test": "jest",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "next": "^15.1.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "@supabase/supabase-js": "^2.45.0",
    "@supabase/ssr": "^0.5.0",
    "openai": "^4.70.0",
    "react-hook-form": "^7.54.0",
    "@hookform/resolvers": "^3.9.1",
    "zod": "^3.24.1",
    "zustand": "^5.0.2",
    "date-fns": "^4.1.0",
    "react-hot-toast": "^2.4.1",
    "lucide-react": "^0.468.0",
    "clsx": "^2.1.1",
    "tailwind-merge": "^2.6.0"
  },
  "devDependencies": {
    "@types/node": "^22",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "typescript": "^5",
    "tailwindcss": "^4.0.0",
    "eslint": "^9",
    "eslint-config-next": "^15.1.0",
    "prettier": "^3.4.2",
    "jest": "^29.7.0",
    "@testing-library/react": "^16.1.0",
    "playwright": "^1.49.1",
    "@sentry/nextjs": "^8"
  }
}
```

---

## 10. 결론

### 10.1 핵심 기술 선정 근거

1. **Next.js 15 + Supabase**: 빠른 개발, 확장성, 보안
2. **OpenAI GPT-4o-mini**: 저렴한 비용, 빠른 응답, 한국어 지원
3. **Vercel**: 자동 배포, Edge Network, 무료 티어

### 10.2 확장 계획

- **Phase 2**: Vector DB (RAG) 도입으로 정확도 향상
- **Phase 3**: Mobile App (React Native) 개발
- **Phase 4**: 전문가 상담 연결, 상품 추천

---

**작성일**: 2026-02-07
**작성자**: BebeCare Team
